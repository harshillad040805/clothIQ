<!DOCTYPE html>
<html>
<head>
    <title>Virtual Try On</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin-top: 50px;
        }
        img {
            max-width: 300px;
            border-radius: 10px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h1>Virtual Try-On Section</h1>
    <% if (imagePath) { %>
        <p>Your uploaded image:</p>
        <img src="<%= imagePath %>" alt="Selfie">
    <% } else { %>
        <p>No image uploaded yet.</p>
    <% } %>
</body>
</html>
